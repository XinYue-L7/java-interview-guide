BIO,NIO,AIO 总结
Java 中的 BIO、NIO和 AIO 理解为是 Java 语言对操作系统的各种 IO 模型的封装。程序员在使用这些
API 的时候，不需要关心操作系统层面的知识，也不需要根据不同操作系统编写不同的代码。只需要使
n 用Java的API就可以了。
c
在讲 BIO,NIO,AIO 之前先来回顾一下这样几个概念：同步与异步，阻塞与非阻塞。
e n
关于同步和异步的概念解读困扰着很多程序员，大部分的解读都会带有自己的一点偏见。参考了
d c
Stackoverflow相关问题后对原有答案进行了进一步完善：
o e n
When you execute something synchronously, you wait for it to finish before moving on to
c d c
another task. When you execute something asynchronously, you can move on to another
理 f task before it finisohes. e n
l c d c
当你同步执行某项任务时，你需要等待其完成才能继续执行其他任务。当你异步执行某些操作
整 o 理 时，你可f以在完成另一个任务之前继续进行。 o e n
w l c d c
同步 ：两个同步任务相互依赖，并且一个任务必须以依赖于另一任务的某种方式执行。 比如在A-
育 ： 整 o>B事件模型中，你需要先完成 A 才能执行B。 再换句理 话说，同步调用种f被调用者未处理完请求之 o e
w 前，调用不返回，调用者会一直等待结果的返回。 l c d
异步： 两个异步的任务完全独立的，一方的执行不需要等待另外一方的执行。再换句话说，异步
教 号 育 ： 整 o 理 f o
调用种一调用就返回结果不需要等待结果返回，当结果返回的时候通过回调函数或者其他方式拿着
w l c
结果再做相关事情，
狼 众 教 号 育 ： 整 o 理 f
阻塞和非阻塞
w l
阻塞： 阻塞就是发起一个请求，调用者一直等待请求结果返回，也就是当前线程会被挂起，无法
丁 公 狼 众 教 号 育 ： 整 o
从事其他任务，只有当条件就绪才能继续。
w
非阻塞： 非阻塞就是发起一个请求，调用者不用一直等着结果返回，可以先去干其他事情。
叩 信 丁 公 狼 众 教 号 育 ：
如何区分 “同步/异步 ”和 “阻塞/非阻塞” 呢？
同步/异步是从行为角度描述事物的，而阻塞和非阻塞描述的当前事物的状态（等待调用结果时的状
微 叩 信 丁 公 狼 众 教 号
态）。
1. BIO (Blocking I/O)
微 叩 信 丁 公 狼 众
同步阻塞I/O模式，数据的读取写入必须阻塞在一个线程内等待其完成。
微 叩 信 丁 公
1.1 传统 BIO
微 叩 信
BIO通信（一请求一应答）模型图如下(图源网络，原出处不明)：
微
n
c
e n
d c
o e n
c d c
理 f o e n
l c d c
整 o 理 f o e n
w l c d c
育 ： 整 o 理 f o e
采用 BIO 通信模型 的服务端，通常由一个独立的 Acceptor 线程负责监听客户端的连接。我们一般通过
w l c d
在while(true) 循环中服务端会调用 accept() 方法等待接收客户端的连接的方式监听请求，请求一
教 号 育 ： 整 o 理 f o
旦接收到一个连接请求，就可以建立通信套接字在这个通信套接字上进行读写操作，此时不能再接收其
他客户端连接请求，只能等待同当前连接的客户端的操作执行w完成， 不过可以通过多线程来支持多个客 l c
户端的连接，如上图所示。
狼 众 教 号 育 ： 整 o 理 f
如果要让 BIO 通信模型 能够同时处理多个客户端请求，就必须使用多线程（主要原因是 w l
socket.accept()、socket.read()、socket.write() 涉及的三个主要函数都是同步阻塞的），
丁 公 狼 众 教 号 育 ： 整 o
也就是说它在接收到客户端连接请求之后为每个客户端创建一个新的线程进行链路处理，处理完成之
w
后，通过输出流返回应答给客户端，线程销毁。这就是典型的 一请求一应答通信模型 。我们可以设想
叩 信 丁 公 一下如果这个连接不做狼 任何事情的话就会众 造成不必要的线程开销，不过可以通过 线程池机制 改教 善，线 号 育 ：
程池还可以让线程的创建和回收成本相对较低。使用FixedThreadPool 可以有效的控制了线程的最大
数量，保证了系统有限的资源的控制，实现了N(客户端请求数量):M(处理客户端请求的线程数量)的伪异
微 叩 信 丁 公 狼 众 教 号
步I/O模型（N 可以远远大于 M），下面一节"伪异步 BIO"中会详细介绍到。
我们再设想一下当客户端并发访问量增加后这种模型会出现什么问题？
微 叩 信 丁 公 狼 众
在 Java 虚拟机中，线程是宝贵的资源，线程的创建和销毁成本很高，除此之外，线程的切换成本也是
很高的。尤其在 Linux 这样的操作系统中，线程本质上就是一个进程，创建和销毁线程都是重量级的系
微 叩 信 丁 公
统函数。如果并发访问量增加会导致线程数急剧膨胀可能会导致线程堆栈溢出、创建新线程失败等问
题，最终导致进程宕机或者僵死，不能对外提供服务。
微 叩 信
1.2 伪异步 IO
为了解决同步阻塞I/O面临的一个链路需要一个线程处理的问题，后来有人对它的线程模型进行了优化 微
一一一后端通过一个线程池来处理多个客户端的请求接入，形成客户端个数M：线程池最大线程数N的
比例关系，其中M可以远远大于N.通过线程池可以灵活地调配线程资源，设置线程的最大值，防止由于
海量并发接入导致线程耗尽。
伪异步IO模型图(图源网络，原出处不明)：
n
c
e n
d c
o e n
c d c
理 f o e n
l c d c
整 o 理 f o e n
w l c d c
育 ： 整 o 理 f o e
采用线程池和任务队列可以实现一种叫做伪异步的 I/O 通信框架，它的模型图如上图所示。当有新的客
w l c d
户端接入时，将客户端的 Socket 封装成一个Task（该任务实现java.lang.Runnable接口）投递到后端
教 号 育 ： 的线程池中进行处理，JDK 的线程池维护一个消息整 队列和 N 个活跃o线程，对消息队列中的任务进行处 理 f o
理。由于线程池可以设置消息队列的大小和最大线程数，因此w，它的资源占用是可控的，无论多少个客 l c
户端并发访问，都不会导致资源的耗尽和宕机。
狼 众 教 号 育 ： 整 o 理 f
伪异步I/O通信框架采用了线程池实现，因此避免了为每个请求都创建一个独立线程造成的线程资源耗 w l
尽问题。不过因为它的底层仍然是同步阻塞的BIO模型，因此无法从根本上解决问题。
丁 公 狼 众 教 号 育 ： 整 o
1.3 代码示例 w
叩 信 丁 公 狼 众 教 号 育 ：
下面代码中演示了BIO通信（一请求一应答）模型。我们会在客户端创建多个线程依次连接服务端并向
其发送"当前时间+:hello world"，服务端会为每个客户端线程创建一个线程来处理。代码示例出自闪电
微 叩 信 侠的博客，原丁 地址如下： 公 狼 众 教 号
https://www.jianshu.com/p/a4e03835921a
微 叩 信 丁 公 狼 众
客户端
/** 微 叩 信 丁 公
*
* @author 闪电侠
微 叩 信
* @date 2018年10月14日
* @Description:客户端
*/
微
public class IOClient {
public static void main(String[] args) {
// TODO 创建多个线程，模拟多个客户端连接服务端
new Thread(() -> {
try {
Socket socket = new Socket("127.0.0.1", 3333);
while (true) {
try {
socket.getOutputStream().write((new Date() + ": hello
world").getBytes());
Thread.sleep(2000);
} catch (Exception e) {
}
}
} catch (IOException e) {
}
n }).start();
c
}
e n
d } c
o e n
服务端
c d c
理 f o e n
/**
l * @author 闪 c电侠 d c
* @date 2018年10月14日
整 o 理 f o e n
* @Description: 服务端
w */ l c d c
public class IOServer {
育 ： 整 o 理 f o e
w public static void main(String[] args) throws IOExceptlion { c d
// TODO 服务端处理客户端连接请求
教 号 育 ： 整 o 理 f o
ServerSocket serverSocket = new ServerSocket(3333);
w l c
狼 众 教 号 // 接收到客户端连接请求之后为每育个客户端创建一个新： 的线程进行链路处理 整 o 理 f
new Thread(() -> {
w l
while (true) {
丁 公 狼 众 try { 教 号 育 ： 整 o
// 阻塞方法获取新的连接
w
Socket socket = serverSocket.accept();
叩 信 丁 公 狼 众 教 号 育 ：
// 每一个新的连接都创建一个线程，负责读取数据
new Thread(() -> {
微 叩 信 丁 公 狼 众 教 号
try {
int len;
byte[] data = new byte[1024];
微 叩 信 丁 公 狼 众
InputStream inputStream = socket.getInputStream();
// 按字节流方式读取数据
微 while ((len = inputStream.read(data)) 叩 != -1) { 信 丁 公
System.out.println(new String(data, 0, len));
}
} catch (IOException e) { 微 叩 信
}
}).start();
微
} catch (IOException e) {
}
}
}).start();
}
}
1.4 总结
在活动连接数不是特别高（小于单机1000）的情况下，这种模型是比较不错的，可以让每一个连接专注
于自己的 I/O 并且编程模型简单，也不用过多考虑系统的过载、限流等问题。线程池本身就是一个天然
的漏斗，可以缓冲一些系统处理不了的连接或请求。但是，当面对十万甚至百万级连接的时候，传统的
BIO 模型是无能为力的。因此，我们需要一种更高效的 I/O 处理模型来应对更高的并发量。
2. NIO (New I/O)
n
c
2.1 NIO 简介
e n
d c
NIO是一种同步非阻塞的I/O模型，在Java 1.4 中引入了 NIO 框架，对应 java.nio 包，提供了 Channel ,
o Selector，Buffer等抽象。 e n
c NIO中的N可以理解为Nond-blocking，不单纯是New。它支持面向缓冲的，基于通道的I/O操作方法c。
理 f NIO提供了与传统BIOo模型中的 Socket 和 ServerSocket 相对应的 SocketChannel 和 e n
ServerSocketChannel 两种不同的套接字通道实现,两种通道都支持阻塞和非阻塞两种模式。阻塞模式
l c d c
使用就像传统中的支持一样，比较简单，但是性能和可靠性都不好；非阻塞模式正好与之相反。对于低
整 o 理 f o e n
负载、低并发的应用程序，可以使用同步阻塞I/O来提升开发速率和更好的维护性；对于高负载、高并
w 发的（网l络）应用，应使用 NIO 的非阻塞模式来开发。 c d c
育 ： 整 o 理 f o e
2.2 NIO的特性/NIO与IO区别
w l c d
教 号 育 ： 如果是在面试中回答这个问题，我觉得首先肯定要整 从 NIO 流是非阻o塞 IO 而 IO 流是阻塞 IO 说起。然 理 f o
后，可以从 NIO 的3个核心组件/特性为 NIO 带来的一些改进来分析。如果，你把这些都回答上了我觉
w l c
得你对于 NIO 就有了更为深入一点的认识，面试官问到你这个问题，你也能很轻松的回答上来了。
狼 众 教 号 育 ： 整 o 理 f
1)Non-blocking IO（非阻塞IO） w l
丁 公 狼 众 教 号 育 ： 整 o
IO流是阻塞的，NIO流是不阻塞的。
w
Java NIO使我们可以进行非阻塞IO操作。比如说，单线程中从通道读取数据到buffer，同时可以继续做
叩 信 丁 公 狼 众 教 号 育 ：
别的事情，当数据读取到buffer中后，线程再继续处理数据。写数据也是一样的。另外，非阻塞写也是
如此。一个线程请求写入一些数据到某通道，但不需要等待它完全写入，这个线程同时可以去做别的事
情。
微 叩 信 丁 公 狼 众 教 号
Java IO的各种流是阻塞的。这意味着，当一个线程调用 read() 或 write() 时，该线程被阻塞，直到
有一些数据被读取，或数据完全写入。该线程在此期间不能再干任何事情了
微 叩 信 丁 公 狼 众
2)Buffer(缓冲区)
微 叩 信 丁 公
IO 面向流(Stream oriented)，而 NIO 面向缓冲区(Buffer oriented)。
Buffer是一个对象，它包含一些要写入或者要读出的数据。在NIO类库中加入Buffer对象，体现了新库
微 叩 信
与原I/O的一个重要区别。在面向流的I/O中·可以将数据直接写入或者将数据直接读到 Stream 对象中。
虽然 Stream 中也有 Buffer 开头的扩展类，但只是流的包装类，还是从流读到缓冲区，而 NIO 却是直
接读到 Buffer 中进行操作。 微
在NIO厍中，所有数据都是用缓冲区处理的。在读取数据时，它是直接读到缓冲区中的; 在写入数据
时，写入到缓冲区中。任何时候访问NIO中的数据，都是通过缓冲区进行操作。
最常用的缓冲区是 ByteBuffer,一个 ByteBuffer 提供了一组功能用于操作 byte 数组。除了ByteBuffer,
还有其他的一些缓冲区，事实上，每一种Java基本类型（除了Boolean类型）都对应有一种缓冲区。
3)Channel (通道)
NIO 通过Channel（通道） 进行读写。
通道是双向的，可读也可写，而流的读写是单向的。无论读写，通道只能和Buffer交互。因为 Buffer，
通道可以异步地读写。
4)Selector (选择器)
NIO有选择器，而IO没有。
选择器用于使用单个线程处理多个通道。因此，它需要较少的线程来处理这些通道。线程之间的切换对
n 于操作系统来说是昂贵的。 因此，为了提高系统效率选择器是有用的。
c
e n
d c
o e n
c d c
理 f o e n
l c d c
整 o 理 f o e n
w l c d c
育 ： 整 o 理 f o e
w l c d
教 号 育 ： 整 o 理 f o
2.3 NIO 读数据和写数据方式 w l c
狼 众 教 号 育 ： 整 o 理 f
通常来说NIO中的所有IO都是从 Channel（通道） 开始的。
w l
从通道进行数据读取 ：创建一个缓冲区，然后请求通道读取数据。
丁 公 狼 众 教 号 育 ： 整 o
从通道进行数据写入 ：创建一个缓冲区，填充数据，并要求通道写入数据。
w
数据读取和写入操作图示：
叩 信 丁 公 狼 众 教 号 育 ：
微 叩 信 丁 公 狼 众 教 号
微 叩 信 丁 公 狼 众
微 叩 信 丁 公
微 叩 信
2.4 NIO核心组件简单介绍
微
NIO 包含下面几个核心的组件：
Channel(通道)
Buffer(缓冲区)
Selector(选择器)
整个NIO体系包含的类远远不止这三个，只能说这三个是NIO体系的“核心API”。我们上面已经对这三个
概念进行了基本的阐述，这里就不多做解释了。
2.5 代码示例
代码示例出自闪电侠的博客，原地址如下：
https://www.jianshu.com/p/a4e03835921a
客户端 IOClient.java 的代码不变，我们对服务端使用 NIO 进行改造。以下代码较多而且逻辑比较复
杂，大家看看就好。
/**
n *
* @author 闪电侠
c
* @date 2019年2月21日
e * @Description: NIO 改造后的服务n端
*/
d c
public class NIOServer {
o e n
public static void main(String[] args) throws IOException {
c // 1. serverSeldector负责轮询是否有新的连接，服务端监测到新的连接之后，不再创建一个新c的线
程，
理 f o e n
// 而是直接将新连接绑定到clientSelector上，这样就不用 IO 模型中 1w 个 while 循环在死
l 等 c d c
Selector serverSelector = Selector.open();
整 o 理 f o e n
// 2. clientSelector负责轮询连接是否有数据可读
w l c d c
Selector clientSelector = Selector.open();
育 ： 整 o 理 f o e
new Thread(() -> {
w l c d
try {
教 号 育 ： // 对应IO编程中服务端启动 整 o 理 f o
ServerSocketChannel listenerChannel = ServerSocketChannel.open();
w l c
listenerChannel.socket().bind(new InetSocketAddress(3333));
狼 众 教 号 listenerChannel.config育ureBlocking(fal： se); 整 o 理 f
listenerChannel.register(serverSelector, SelectionKey.OP_ACCEPT); w l
丁 公 狼 众 教 号 育 ： 整 o
while (true) {
// 监测是否有新的连接，这里的1指的是阻塞的时间为 1ms w
if (serverSelector.select(1) > 0) {
叩 信 丁 公 狼 众 教 号 育 ：
Set<SelectionKey> set = serverSelector.selectedKeys();
Iterator<SelectionKey> keyIterator = set.iterator();
微 叩 信 丁 公 狼 众 教 号
while (keyIterator.hasNext()) {
SelectionKey key = keyIterator.next();
微 叩 信 丁 公 狼 众
if (key.isAcceptable()) {
try {
微 // (1) 每来一个新连接，不需要创建一个线叩程，而是直接注册到信 丁 公
clientSelector
SocketChannel clientChannel = ((ServerSocketChannel)
微 叩 信
key.channel()).accept();
clientChannel.configureBlocking(false);
clientChannel.register(clientSelector, SelectionKey.OP_READ);
微
} finally {
keyIterator.remove();
}
}
}
}
}
} catch (IOException ignored) {
}
}).start();
new Thread(() -> {
try {
while (true) {
// (2) 批量轮询是否有哪些连接有数据可读，这里的1指的是阻塞的时间为 1ms
if (clientSelector.select(1) > 0) {
Set<SelectionKey> set = clientSelector.selectedKeys();
Iterator<SelectionKey> keyIterator = set.iterator();
n
while (keyIterator.hasNext()) {
c
SelectionKey key = keyIterator.next();
e n
d if (key.isReacdable()) {
try {
o e n
SocketChannel clientChannel = (SocketChannel) key.channel();
c BdyteBuffer byteBuffer = ByteBuffer.allocate(1024); c
// (3) 面向 Buffer
理 f o e n
clientChannel.read(byteBuffer);
l c byteBuffer.flip(); d c
System.out.println(
整 o 理 f o e n
w l c d c
Charset.defaultCharset().newDecoder().decode(byteBuffer).toString());
育 ： 整 o } finally { 理 f o e
keyIterator.remove();
w l c d
key.interestOps(SelectionKey.OP_READ);
教 号 育 ： } 整 o 理 f o
}
w l c
狼 众 教 号 育 ： 整 o 理 f
}
} w l
}
丁 公 狼 众 教 号 育 ： 整 o
} catch (IOException ignored) {
} w
}).start();
叩 信 丁 公 狼 众 教 号 育 ：
}
}
微 叩 信 丁 公 狼 众 教 号
为什么大家都不愿意用 JDK 原生 NIO 进行开发呢？从上面的代码中大家都可以看出来，是真的难用！
微 叩 信 丁 公 狼 众
除了编程复杂、编程模型难之外，它还有以下让人诟病的问题：
JDK 的 NIO 底层由 epoll 实现，该实现饱受诟病的空轮询 bug 会导致 cpu 飙升 100%
微 叩 信 丁 公
项目庞大之后，自行实现的 NIO 很容易出现各类 bug，维护成本较高，上面这一坨代码我都不能
保证没有 bug
微 叩 信
Netty 的出现很大程度上改善了 JDK 原生 NIO 所存在的一些让人难以忍受的问题。
3. AIO (Asynchronous I/O)
微
AIO 也就是 NIO 2。在 Java 7 中引入了 NIO 的改进版 NIO 2,它是异步非阻塞的IO模型。异步 IO 是基于
事件和回调机制实现的，也就是应用操作之后会直接返回，不会堵塞在那里，当后台处理完成，操作系
统会通知相应的线程进行后续的操作。
AIO 是异步IO的缩写，虽然 NIO 在网络操作中，提供了非阻塞的方法，但是 NIO 的 IO 行为还是同步
的。对于 NIO 来说，我们的业务线程是在 IO 操作准备好时，得到通知，接着就由这个线程自行进行 IO
操作，IO操作本身是同步的。（除了 AIO 其他的 IO 类型都是同步的，这一点可以从底层IO线程模型解
释，推荐一篇文章：《漫话：如何给女朋友解释什么是Linux的五种IO模型？》 ）
查阅网上相关资料，我发现就目前来说 AIO 的应用还不是很广泛，Netty 之前也尝试使用过 AIO，不过
又放弃了。
参考
《Netty 权威指南》第二版
https://zhuanlan.zhihu.com/p/23488863 (美团技术团队)
n
c
e n
d c
o e n
c d c
理 f o e n
l c d c
整 o 理 f o e n
w l c d c
育 ： 整 o 理 f o e
w l c d
教 号 育 ： 整 o 理 f o
w l c
狼 众 教 号 育 ： 整 o 理 f
w l
丁 公 狼 众 教 号 育 ： 整 o
w
叩 信 丁 公 狼 众 教 号 育 ：
微 叩 信 丁 公 狼 众 教 号
微 叩 信 丁 公 狼 众
微 叩 信 丁 公
微 叩 信
微
