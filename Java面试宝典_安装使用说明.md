# Java 面试宝典 · 手机/iPad 安装与离线使用说明

本项目的运行文件已打包为 **`Java面试宝典_离线安装包.zip`**（30 个文件，约 320KB）。
它是纯静态网站 + **PWA（渐进式 Web 应用）**：首次联网打开一次后，**全部 22 个页面会被自动缓存，之后完全离线使用**，无需网络。

---

## 一、推荐方式：PWA 安装（安卓 / iPad 通用）

> 重要前提：PWA 的离线缓存（Service Worker）只在 **HTTPS** 环境下生效。
> 因此需要先把网站发布到一个 HTTPS 地址（免费托管即可），再在手机上安装。

### 第 1 步：发布网站到 HTTPS 地址（三选一）

**方式 A：GitHub Pages（免费，推荐）**
1. 在 GitHub 新建仓库，把 ZIP 里的 30 个文件上传到仓库根目录（保持文件在根目录，不要放子文件夹）。
2. 仓库 Settings → Pages → Source 选 main 分支 → Save。
3. 等待 1-2 分钟，即可得到网址：`https://你的用户名.github.io/仓库名/Java面试宝典_总目录.html`

**方式 B：腾讯云 CloudBase / EdgeOne Pages（免费，国内快）**
1. 打开 CloudBase 静态网站托管控制台，新建环境。
2. 把 ZIP 解压后的 30 个文件全部上传到根目录。
3. 开通后得到 `https://xxxx.tcloudbaseapp.com` 或类似 HTTPS 域名，找到 `Java面试宝典_总目录.html` 即为入口。

**方式 C：Vercel（免费）**
1. 打开 vercel.com，用 GitHub 账号登录，Import 仓库。
2. Framework 选 "Other"，直接 Deploy，得到 `https://xxxx.vercel.app` HTTPS 地址。

> 发布后先在电脑浏览器打开确认页面正常，再继续下一步。

### 第 2 步：安卓手机安装（Chrome 浏览器）

1. 手机 Chrome 打开你的 HTTPS 入口地址（总目录页）。
2. 等待 2-3 秒让页面完成缓存（此时可断网验证，见"离线验证"）。
3. 点击浏览器右上角 **⋮ 菜单 → 「安装应用 / 安装 App」**（部分手机叫"添加到主屏幕"）。
4. 桌面出现 **「面试宝典」图标**，点击即可全屏打开，**无需浏览器、无需网络**。

> 提示：图标来自 `icon-512.png` / `icon-192.png`，已配置好。

### 第 3 步：iPad / iPhone 安装（Safari 浏览器）

1. iPad 用 **Safari** 打开你的 HTTPS 入口地址。
2. 等待几秒完成缓存。
3. 点击底部 **分享按钮（方框+箭头）→ 「添加到主屏幕」**。
4. 确认名称"面试宝典"，点击**添加**。
5. 桌面出现 **「面试宝典」图标**，点击全屏打开，**离线可用**。

> iOS 已配置 `apple-touch-icon.png`（180×180）与独立模式 meta，图标和全屏效果都正常。

### 离线验证

1. 安装后，把手机/iPad 开启**飞行模式**（或关闭 WiFi）。
2. 点击桌面「面试宝典」图标 → 页面正常打开、所有题目可浏览可搜索，即安装成功。

---

## 二、临时方案：局域网访问（不适合长期离线）

项目自带本地服务（`启动服务.bat`，端口 9876）。
手机与电脑连**同一 WiFi**，浏览器打开电脑 IP：`http://电脑IP:9876/` 即可浏览。

**注意：** 局域网是 HTTP，浏览器会禁止离线缓存，**断开网络后无法使用**。
若只是"在家临时看看"可以，但**长期离线使用请用上面的 PWA 方案**。

---

## 三、打包文件清单（ZIP 内容）

| 文件 | 说明 |
|---|---|
| `Java面试宝典_总目录.html` | 入口页（请发布时直接访问此页） |
| `Java面试宝典_01~08、Spring、MyBatis 等 22 个模块页` | 全部题库页面 |
| `sw.js` | 离线缓存核心（安装时预缓存全部页面） |
| `manifest.json` | PWA 清单（应用名称、图标） |
| `icon-192.png` / `icon-512.png` / `apple-touch-icon.png` / `icon.svg` | 各平台应用图标 |
| `search_index.json` | 站内搜索索引（已同步 390 题） |
| `qrcode.min.js` | 二维码库（供首页功能使用） |

---

## 四、常见问题

**Q1：安装后打开提示"已离线"？**
说明首次打开时缓存未完成。请联网重新打开一次总目录页，停留 3-5 秒，再断网测试。

**Q2：地址栏能安装，但没有"安装应用"选项？**
确认用的是 HTTPS 地址（`https://` 开头），且浏览器是 Chrome（安卓）或 Safari（iOS）。

**Q3：以后题库更新了怎么办？**
重新发布 ZIP 里的新文件到同一地址，手机联网打开一次即自动更新缓存（Service Worker 已按版本号升级机制设计）。

**Q4：能不能做成真正的 APK / iOS App？**
可以，但需要开发者账号或 Android SDK 构建工具（本机环境不具备）。
如果你确实需要独立的 .apk 文件，可以后续用 Capacitor 或 Android WebView 打包，需要时再告诉我。
